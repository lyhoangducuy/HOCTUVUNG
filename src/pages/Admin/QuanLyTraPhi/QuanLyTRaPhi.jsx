import MainContentQLTP from "./MainContentQLTP/MainConTentQLTP";

const QuanLyTraPhi = () => {
  // Dữ liệu sẽ được MainContentQLTP tự load từ localStorage → không cần truyền props
  return <MainContentQLTP />;
};

export default QuanLyTraPhi;
